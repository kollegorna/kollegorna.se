  <article class="block block--case-study <%= defined?(full_width) ? "full" : "" %>" style="background: <%= defined?(background_color) ? background_color : "#000000" %>; color: <%= defined?(text_color) ? text_color : "#FFFFFF" %>;">
    <a href="<%= defined?(link) ? link : "#" %>">
      <div class="block__text">
        <p class="category" style="border-color: <%= defined?(text_color) ? text_color : "#000000" %>;">
          <% if defined?(category) %>
            <%= category %>
          <% else %>
            <%= t(:cases)[:title] %>
          <% end %>
        </p>
        <h3><%= title %></h3>
        <p><%= text %></p>
      </div>

      <% if defined?(image) %>
        <div class="block__image">
          <%= image_tag(image) %>
        </div>
      <% end %>
    </a>
  </article>
